import ToggleThemeButton from "@/components/toggle-theme";

export default function Header() {
  return (
    <header className="fp-w-full fp-flex fp-justify-end">
      <ToggleThemeButton />
    </header>
  );
}
